services:
  postgres:
    image: postgres:16
    container_name: signalos_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: signalos
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: signalos_db
    ports:
      - "5432:5432"